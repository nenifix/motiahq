{
  "name": "motiahq",
  "version": "1.0.0",
  "description": "motiahq — Agent Orchestration HQ. Developed by nenifix.com",
  "main": "src/server/index.js",
  "scripts": {
    "start": "node src/server/index.js serve",
    "dev": "node --watch src/server/index.js serve",
    "build:ui": "cd src/ui && npm ci && npm run build && cp -r dist ../ui-dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-windows": "^1.0.0-beta.8"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
